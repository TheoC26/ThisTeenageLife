import React from "react";

export const metadata = {
  title: "TTL - Educational Resources",
  description:
    "Welcome to the educational resource page. Here you can find all of our recources, which allow you to continue to explore topics we cover in our episodes.",
};

const Layout = ({ children }) => {
  return children;
};

export default Layout;
